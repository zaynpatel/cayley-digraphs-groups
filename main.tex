\documentclass{beamer}
\usepackage{amsfonts}
\usepackage{color,soul}
\usepackage{graphicx}
\usepackage{tikz}
\graphicspath{ {images/} }
\usepackage{caption}
\usepackage{subcaption}
%Information to be included in the title page:
\title{Groups, graphs, and morphisms}
\author{Zayn Patel \\ \texttt{zaynpatelwhs@gmail.com}}
\date{May 21, 2026}
\setbeamertemplate{navigation symbols}{}

\begin{document}

\frame{\titlepage}

\begin{frame}
\frametitle{Goal}
This talk demonstrates a relationship between \textbf{group} theory and \textbf{graph} theory. \bigskip

To do this we will introduce Cayley DiGraphs of Groups which will allow us to visualize a group as a directed graph. \bigskip

This visualization can help us understand properties of a group (e.g. the relations) but we will discover that Cayley DiGraphs give us many valuable theorems about groups we like (e.g. $\mathbb{Z}_{m} \oplus \mathbb{Z}_{n})$.
\end{frame}

\begin{frame}
\frametitle{Topics covered}
To accomplish our goal, we will cover the following: \bigskip
    \begin{itemize}
        \item Generators and relations
        \item Cayley DiGraphs of Groups
        \item Hamiltonicity of DiGraphs
        \item Graph theory meets groups theory
        \item Generators and relations rigor
    \end{itemize}
\end{frame}

\begin{frame}
\frametitle{Generators and relations}
    \begin{itemize}
        \item<1-> Generators and relations are special elements we can combine, via the group operation, to get any other element in the group.
        \item<2-> Groups with a single element that combines with itself to generate the entire group are called \textbf{cyclic}. Recall that we can also have subgroups which are cyclic.
        \item<3-> Most groups we have worked with are not cyclic and have multiple generators. $\mathbb{Z}$, for example, has $\pm 1$ as a generator. $D_{4}$ has $R_{90}$ and $H$ as generators. 
        \item<4-> We call the set of generators of G the \textbf{generating set} which is usually called S. For $D_{4}$, $S = \{R_{90}, H\}$.
    \end{itemize}
\end{frame}

\begin{frame}
\frametitle{Generators and relations}
    \begin{itemize}
        \item<1-> Relation definition: the smallest set of equations that specify the conditions the generators must satisfy. In $D_{4}$ $R^{4} = H^{2} = (RH)^{2} = R_{0}$.
        \item<2-> Because this is the minimal set we can derive all additional relations from this set.
        \item<3-> If two groups are defined by the same relations, they are isomorphic.
        \item<4-> Describing a group by its generators and relations is called a \textbf{group presentation}. The presentation for $D_{4}$ is $\langle R, H | R^{4} = H^{2} = (RH)^{2} = R_{0}\rangle$.
        \item<5-> In order to define generators and relations rigorously, we need a beautiful concept called free groups. More information is available in the appendix if you are interested.
    \end{itemize}
\end{frame}

\begin{frame}
\frametitle{Cayley Digraphs of Groups}

\textit{Purpose:} Visualize a group given by generators and relations

These are directed graphs where vertices are group elements and arcs, which are the generators, connect the vertices. \medskip

\textit{Note:} Edges are typically used to describe vertex connections in \textbf{undirected} graphs while arcs describe connections in \textbf{directed graphs}.

\begin{figure}
    \centering
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/Z_6_DiGraph.png}
        \caption{$\mathbb{Z}_{6}$ DiGraph}
        \label{fig:Z_6 DiGraph}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/A_4_DiGraph.png}
        \caption{$A_{4}$ DiGraph}
        \label{fig:A_4 DiGraph}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/S_3.png}
        \caption{$S_{3}$ DiGraph}
        \label{fig:Z_6 DiGraph}
    \end{subfigure}
\end{figure}
\end{frame}

\begin{frame}
\frametitle{Cayley Digraphs of Groups}

Example 8 shows us that we can make DiGraphs for infinite groups too.


\begin{figure}
    \centering
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/Z_3_Z_2.png}
        \caption{$\mathbb{Z}_{3} \oplus \mathbb{Z}_{2}$ DiGraph}
        \label{fig:Z_3 x Z_2 DiGraph}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/D_4_DiGraph.png}
        \caption{$D_{4}$ DiGraph}
        \label{fig:D_4 DiGraph}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/Infinite_DiGraph.png}
        \caption{Infinite DiGraph}
        \label{fig: Infinite Vertices}
    \end{subfigure}
\end{figure}
\end{frame}

\begin{frame}
\frametitle{Cayley Digraphs of Groups}
\begin{block}{Definition: Cayley Digraph of a Group}
Let G be a finite group and let S be a set of generators for G. We define a digraph Cay(S:G), called the Cayley digraph of G with the generating set S, as follows:
    \begin{enumerate}
        \item Each element of G is a vertex of Cay(S:G)
        \item For each x and y in G, there is an arc from x to y if and only if xs = y for some s $\in$ S.
    \end{enumerate}
\end{block}
\end{frame}

\begin{frame}
\frametitle{Cayley Digraphs of Groups}
\begin{block}{Example}
Let's look at $\mathbb{Z}_{3} \oplus \mathbb{Z}_{2}$ as an example. We need to check the first condition: each element of G is a vertex of Cay(S:G). \smallskip

$\mathbb{Z}_{3} \oplus \mathbb{Z}_{2}$ = \{(0, 0), (0, 1), (1, 0), (1, 1), (2, 0), (2, 1)\} \smallskip

\begin{figure}[h]
\centering
\includegraphics[scale=0.5]{images/Z_3_Z_2.png}
\caption{$\mathbb{Z}_{3} \oplus \mathbb{Z}_{2}$ DiGraph}
\end{figure}
\end{block}

\end{frame}

\begin{frame}{Cayley DiGraphs of Groups}
The second condition says that $\exists$ arc iff an element of the group multiplied by some generator produces a new element of the group. \smallskip
Some examples of this in $\mathbb{Z}_{3} \oplus \mathbb{Z}_{2}$ are: \smallskip

$(2, 0) \cdot (1, 0)$ = (2 + 1, 0 + 0) = (3 mod(3), 0 mod(2)) = (0, 0) \smallskip

$(1, 1) \cdot (0, 1)$ = (1 + 0, 1 + 1) = (1 mod(3), 2 mod(2)) = (1, 0)

\begin{figure}[h]
\centering
\includegraphics[scale=0.4]{images/Z_3_Z_2.png}
\caption{$\mathbb{Z}_{3} \oplus \mathbb{Z}_{2}$ DiGraph}
\end{figure}

\end{frame}

\begin{frame}{Cayley DiGraphs of Groups}
How do we find relations for a group and how might a graph help us do this?
\pause

In general, finding the relations for a group is a problem in computational group theory. There are specific algorithms that search the "group space" to find and confirm relations in large groups. We will not cover these today. Instead, we will look at two finite groups whose relations we can see by tracing the arcs.
\end{frame}

\begin{frame}{Cayley DiGraphs of Groups}

We will use $A_{4}$ as an example and let us begin by finding the relations between the generators and the identity element. \smallskip

Let $a$ = (12)(34) and $b$ = (123).

\begin{figure}[h]
\centering
\includegraphics[scale=0.3]{images/A_4_DiGraph.png}
\caption{$A_{4}$ DiGraph}
\end{figure}

\pause
$a^{2} = b^{3} = (ab)^{3} = (1)$
\pause

We can also derive other relations from this set of generators. \smallskip

$(ab)^{3} = ababab = bab = a^{-1}b^{-1}a^{-1} = (124)$

\end{frame}

\begin{frame}{Cayley DiGraphs of Groups}
You may be wondering how to know the complete set of relations for a group. In my understanding, this is an undecidable problem. \medskip
\pause

Gallian mentions that there are cases where one group can be defined with two different sets of generators and relations. So another undecidable problem is determining whether these groups are isomorphic to each other. \medskip
\pause

Lastly, note that defining a group by its presentation does not provide information on the cardinality of the group. Consider the group presentation for $A_{4}: \langle a^{2} = b^{3} = (ab)^{3} = e \rangle$. If we did not know prior information about the cardinality of $A_{4}$ we would not know if it was finite or infinite. \medskip

As another example $\mathbb{Z}$ has the group presentation $\langle x \rangle$ and we know this is infinite.
\end{frame}

\begin{frame}{Graph Theory meets Group Theory}
\begin{block}{Definition: Hamiltonian Circuit and Path}
    \begin{itemize}
        \item<1-> \textit{Hamiltonian Circuit:} Path that starts at a vertex, visits each vertex exactly once, and returns to the starting vertex.
        \item<2-> \textit{Hamiltonian Path:} Path that starts at a vertex and visits each vertex exactly once without returning to the starting vertex.
    \end{itemize}

    \begin{figure}
        \centering
        \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/Z_32_Ham_Path.png}
        \caption{Hamiltonian Path in the $\mathbb{Z}_{3} \oplus \mathbb{Z}_{2}$ DiGraph}
        \label{fig:Z_3 x Z_2 HamPath DiGraph}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/Q_4_Hamil_Circuit.png}
        \caption{Hamiltonian Circuit in the $Q_{4}$ DiGraph}
        \label{fig:Q_{4} DiGraph}
        \end{subfigure}
        \hfill
    \end{figure}
\end{block}
\end{frame}

\begin{frame}{Graph Theory meets Group Theory}
Hamiltonian paths and circuits are not unique; in any given graph we may find multiple paths that satisfy the definition.

There is a condition about Hamiltonian circuits and generators which is important enough to prove.

\textbf{Claim:} If $x_{1}, x_{2}, ..., x_{n}$ is a sequence of generators determining a Hamiltonian circuit, then the same sequence determines a Hamiltonian circuit for any starting vertex. \smallskip

This is profound because ...

\begin{proof}
TODO item.
\end{proof}
\end{frame}

\begin{frame}{More group theory and graph theory connections}
So far we have associated a graph with a group but we can go the other way. That is we can associate a group with every graph. This group is called the automorphism group of a graph.

We will be talking about the automorphism group of a graph so we need to define new terms. From group theory we know that an automorphism is an isomorphism from a group to itself. The set of automorphisms of a group forms a group called the \textbf{automorphism group} denoted Aut(G). The elements of Aut(G) are group isomorphisms. The appendix contains a proof that the automorphism group is a group with under function composition.
\end{frame}

\begin{frame}{Graph Isomorphisms and Automorphisms}
    \begin{block}{Definition: Graph Isomorphisms and Automorphisms}
    \begin{itemize}
    \item<1-> \textit{Graph isomorphism:} An isomorphism between graphs G and H is a bijection $\phi: V(G) \to V(H)$ s.t. $uv \in E(G)$ iff $\phi(u)\phi(v) \in E(H)$. Notice that this defines an isomorphism between the vertex sets of the graphs. In examples, we will see that the degree of the vertex also matters in defining an isomorphism.
    \item<2-> \textit{Graph automorphism:} An isomorphism from a graph to itself. Formally, it is a bijection $\phi: V(G) \to V(G)$ s.t. $uv \in E(G)$ iff $\phi(u)\phi(v) \in E(G)$.
\end{itemize}
\end{block}
\end{frame}

\begin{frame}{Finding the automorphisms of a graph G}
\begin{center}
\begin{tikzpicture}[every node/.style={circle, draw, minimum size=3mm}]
    \node (w) at (1, 2) {w};
    \node (u) at (-2, 0) {u};
    \node (v) at (0, 0) {v};
    \node (x) at (2, 0) {x};
    \node (y) at (4, 0) {y};
    
    \draw (w) -- (v);
    \draw (w) -- (x);
    \draw (u) -- (v);
    \draw (v) -- (x);
    \draw (x) -- (y);
\end{tikzpicture}
\end{center}

We will denote the identity automorphism as $\epsilon$ with the map $u \to u, v \to v, x \to x, y \to y$. \pause

$\alpha$ has the map $u \to y, y \to u, v \to x, x \to v, w \to w$. We map $u \to y$ because these are the only elements of degree one and $v \to x$ because these were adjacent to the elements of degree one. So, Aut(G) = $\{\epsilon, \alpha\}$. \pause

We can compose these using the group operation. But recall that Aut(G) is a group and we know, from group theory, that the non-identity element is its own inverse in groups with two elements. Thus, $\alpha \circ \alpha = \epsilon$.
\end{frame}

\begin{frame}{Finding the automorphisms of a graph G}
Now that we know the elements of Aut(G) and their products we can draw a Cayley table.

\noindent\begin{tabular}{c | c c c}
     & $\epsilon$ & $\alpha$ \\
    \cline{1-3}
    $\epsilon$ & $\epsilon$ & $\alpha$ \\
    $\alpha$ & $\alpha$ & $\epsilon$ \\
\end{tabular}

\smallskip

What well known group with two elements equals the identity when we add the non-identity element to itself? \pause

$\mathbb{Z}_{2}$ has this property because (1 + 1) mod (2) = 0. The Cayley table for $\mathbb{Z}_{2}$ is:

\smallskip
\noindent\begin{tabular}{c | c c c}
     & 0 & 1 \\
    \cline{1-3}
    0 & 0 & 1 \\
    1 & 1 & 0 \\
\end{tabular}

\pause
\medskip

\textbf{Conclusion:} $Aut(G) \cong \mathbb{Z}_{2}$. We have found an automorphism between a graph and a well-known group!
\end{frame}

\begin{frame}{Equivalence relation between two vertices in G}
Let $u, v \in V(G)$. We say $u \sim v$ if $\exists \space \phi \in Aut(G)$ s.t. $\phi(u) = v$. Two vertices are related if there is some graph automorphism that maps one vertex to the other. \pause

\textbf{Consequence:} We can partition the vertex set of a graph G. \smallskip
\pause

\begin{block}{Application: Efficient Graph Automorphism by Vertex Partitioning}
\begin{quote}
The graph automorphism partition has other uses. Since it is in essence an
encoding of the symmetries of the graph, having the automorphism partition
available can be of definite aid ...

Symmetry information contained in the automorphism partition can also be
used to prune backtracking tree searches.
\textcolor{red}{The tree search can maintain the
automorphism partition of the data structures searched thus far. Then, when
new structures are generated, the symmetries in the partition can be used to
detect and eliminate redundant branches in the search.}
\end{quote}
\end{block}

\end{frame}

\begin{frame}{Appendix: Proof that the automorphism group is a group}
\textbf{Claim: The automorphism group is a group.}
    \begin{proof}
        Let Aut(G) be the automorphism group of G. We want to show that this is a group. First we note that the identity map (the map that takes each element of g in the domain to its corresponding g in the codomain) is an element of Aut(G) by definition. This map is 1-1, onto, and operation preserving. Thus we have an identity element of Aut(G).

        Next, let $\varphi: G \to G$, $\lambda: G \to G$ $\in$ Aut(G). We want to show that $(\varphi \circ \lambda)(g) \in G$. $\lambda (g) = g$ in $\lambda: G \to G$. So $g$ is an element of the codomain of $\lambda$ which is the input to $\varphi$. Then $\varphi(g) = g$ by its map. Thus we have closure.

        For inverses, let $\varphi: G \to G \in$ Aut(G). By definition of Aut(G) $\varphi$ is an isomorphism. By Theorem 6.2, property 1 (Gallian 10th edition) $\varphi^{-1}$ exists and is an isomorphism from $G \to G$. Lastly, the associative property is inherited from the definition of function composition.
    \end{proof}
\end{frame}

\end{document}